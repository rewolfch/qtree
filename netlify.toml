
[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  targetPort = 5173

# Catch-all redirect for React Router (Single Page Application Support)
# Das ist wichtig, damit z.B. /blog/mein-artikel beim Neuladen funktioniert.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
